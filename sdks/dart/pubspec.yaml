name: goodbase
description: Official Goodbase Flutter SDK for data, telemetry, config, messaging, and attestation.
version: 0.1.0
homepage: https://base.goodos.app
repository: https://github.com/preps103/GoodOSAppBackEnd
environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"
dependencies:
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.0.0
