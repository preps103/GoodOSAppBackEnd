# Store the production copy at /etc/goodbase/goodspeech.env with mode 0600.
# Generate a unique value with: openssl rand -hex 32
KOKORO_TTS_TOKEN=replace-with-at-least-32-random-characters
KOKORO_CONCURRENCY=1
