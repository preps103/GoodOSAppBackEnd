# Next.js starter

Set `GOODBASE_URL` and `GOODBASE_PUBLIC_KEY`. Exchange and refresh sessions in server-only code, keep HTTP-only cookies out of client components, pass request IDs through Route Handlers, and use the browser client only for public/RLS-scoped calls.
