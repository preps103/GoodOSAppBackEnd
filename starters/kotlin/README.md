# Kotlin starter

Store credentials in Android Keystore-backed storage, request a Play Integrity assertion for the Goodbase challenge, exchange it for a short-lived attestation token, and include it when registering the FCM token. WorkManager should replay the durable mutation queue.
