# Swift starter

Store credentials in Keychain, obtain an App Attest assertion from the server nonce, exchange it for a short-lived Goodbase attestation token, and include that token when registering the APNs device token. Use background tasks for queued offline mutations.
