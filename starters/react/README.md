# React starter

Set `VITE_GOODBASE_URL` and `VITE_GOODBASE_PUBLIC_KEY`, create one `GoodbaseClient`, wrap it with the React provider, and enable IndexedDB sync per signed-in user. Clear the cache during logout and surface pending mutations and conflicts in the UI.
