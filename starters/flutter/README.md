# Flutter starter

Use the Dart SDK with Keychain/Keystore-backed credential storage. Persist the encrypted offline database per user, run background synchronization after connectivity returns, obtain platform attestation, then register the FCM/APNs token.
