# Goodbase updated-audit corrections

The July 21 audit must be read against exact production evidence. The canonical Studio is `https://base.goodos.app/studio`. Every request to the retired backend hostname, including `/` and `/studio`, now returns a method-preserving `308`, `Deprecation`, `Sunset`, and successor link. Required CI and Goodbase Assurance both passed for release `0ecca41bc0a286c2954d1cb8f8d7e0d2993bea4b`; their retained artifacts and GitHub run IDs were verified and recorded by Goodbase.

This correction adds tenant-isolated control planes for Terraform/Pulumi state, locks, plans, drift, imports and exports; signed and reviewed extension packages with permission manifests, secret references, controller-backed installation and rollback; and an attested, quota-bounded, provider-neutral AI gateway with model routing, prompt templates, structured output, tool allowlists, safety rules, metering, conversations and evaluation evidence. Provider records always begin unverified and become ready only after a live probe.

Swift, Kotlin and Flutter/Dart now have durable encrypted offline mutation stores. Each store is isolated by user, persists ordered idempotent mutations atomically, tracks replay cursors, supports schema migration and deletes its data at logout. Certification still requires retained real-device results for background sync, offline restart, large queues, conflicts, missed Realtime replay, multi-device reconciliation, expired tokens, cache encryption and cache migration.

SDK releases cannot activate from source files alone. Activation requires an exact source commit with passing CI evidence, a checksum, signature, SBOM, changelog, compatibility matrix and retained conformance output covering REST, GraphQL, Realtime, offline sync, Storage, authentication, attestation and product telemetry. Production controller certification now requires every named controller family to publish fresh version and capability evidence.

The remaining blocked work is genuinely external: a separate non-production chaos target, controller deployments, off-server backup destinations, a standby database, package-registry and signing identities, real mobile/device labs, a second region, CDN and object replication, app-store distribution, symbol files and live telemetry, a build/hosting provider, GitHub branch-protection administration, billing/tax/support providers, and independent compliance artifacts. These must not be represented by seeded “ready” rows.
